<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Email - BrainElevate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background-color: #2fb5a6;
    }

    .confirm-container {
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .confirm-card {
      background-color: #2fb5a6;
      padding: 3rem;
      text-align: center;
      border-radius: 10px;
      width: 100%;
      max-width: 400px;
    }

    .confirm-card img {
      width: 120px;
      margin-bottom: 20px;
    }

    .confirm-card input[type="email"] {
      width: 100%;
      padding: 10px 15px;
      border-radius: 8px;
      border: 1px solid #ccc;
      margin-bottom: 20px;
      font-size: 16px;
    }

    .confirm-card button {
      width: 100%;
      padding: 10px;
      border: none;
      border-radius: 8px;
      background-color: #0040ff;
      color: white;
      font-size: 16px;
      cursor: pointer;
    }

    .confirm-card button:hover {
      background-color: #002bb3;
    }

    @media (max-width: 576px) {
      .confirm-card {
        padding: 2rem;
      }

      .confirm-card img {
        width: 100px;
      }
    }
  </style>
</head>
<body>
  <div class="confirm-container">
    <div class="confirm-card">
      <img src="/images/logo.png" alt="BrainElevate Logo">
      <h4 class="mb-3">Confirm your Email id</h4>

      <form action="/forgot-password" method="POST"  novalidate>
         <input type="email" name="email" class="form-control" placeholder="Email" value="<%= formData?.email || '' %>" required>
    <% if (fieldErrors?.email) { %>
  <div class="error"><%= fieldErrors.email %></div>
<% } %>
  <input type="hidden" name="type" value="<%= type %>">

        <button type="submit"style="background-color:#3BADCD;">Confirm</button>
      </form>

    </div>
  </div>
</body>
</html>
